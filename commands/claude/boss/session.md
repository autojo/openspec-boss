---
name: "Boss: Session"
description: "Start a boss tab in a project's workspace with the scoped runner config"
allowed-tools: Bash
category: "Boss"
tags: ["boss", "workflow"]
---

Run `boss session $ARGUMENTS` and summarize the JSON result for the user (project, workspace, agent, tab/pane, status). On error, report the error message from stderr.
