---
description: "Start the apply of an OpenSpec change in its own Herdr tab"
---

Run `boss dispatch $ARGUMENTS` and summarize the JSON result for the user. On error, report the error message from stderr.
