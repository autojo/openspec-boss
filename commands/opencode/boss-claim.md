---
description: "Claim this Herdr workspace as boss (or release it)"
---

Run `boss claim $ARGUMENTS` and summarize the JSON result for the user (workspace, agent, status). On error, report the error message from stderr.
